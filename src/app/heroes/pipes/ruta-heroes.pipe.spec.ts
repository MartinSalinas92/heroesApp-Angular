import { RutaHeroesPipe } from './ruta-heroes.pipe';

describe('RutaHeroesPipe', () => {
  it('create an instance', () => {
    const pipe = new RutaHeroesPipe();
    expect(pipe).toBeTruthy();
  });
});
